server:
  port: 8080 # Set the port for our SpringBoot Application

spring:
  profiles:
    active: postgres-db
  datasource:
    url: jdbc:postgresql://ep-muddy-snow-a1se2sp4-pooler.ap-southeast-1.aws.neon.tech/expensedb?sslmode=require
    driver-class-name: org.postgresql.Driver # The JDBC driver for H2
    username: neondb_owner
    password: npg_j1EbdVfr7tLX

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update # Automatically creates or updates the database schema based on entity classes
    show-sql: true # Enables logging of SQL queries generated by JPA
    database-platform: org.hibernate.dialect.PostgreSQLDialect